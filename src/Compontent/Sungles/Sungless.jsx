import add from "../../Utiles/Calculate";

const Sungless = () => {

    const fast = 50;
    const second =120;
    const sum = add(fast,second);
    console.log(sum)

    return (
        <div>
            
        </div>
    );
};

export default Sungless;